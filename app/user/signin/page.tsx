/**
 * Canonical sign-in URL shared in invitation and password-reset emails
 * (https://lms.creovixa.com/user/signin). It renders the same secure login
 * screen as /login so a single, stable link works for every account.
 */
export { default } from "@/app/login/page"
